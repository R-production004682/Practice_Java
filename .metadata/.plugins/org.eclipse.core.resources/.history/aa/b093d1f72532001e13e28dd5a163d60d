import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

public class Sample_02
{
	//入力ができる
	public static void main(String[] ages) throws IOException 
	{
		//2_1＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
		//BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		//String s = br.readLine();
		//System.out.print(s);
		//br.close();
		
		//2_2＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
		//BufferedReader br = new  BufferedReader(new InputStreamReader(System.in));
		//int x = Integer.parseInt(br.readLine());
		//System.out.print(x);
		//br.close();
		
		//2_3＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
		//BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		//
		//int x = Integer.parseInt(br.readLine());
		//int twice = x * x;
		//int three = x * x * x;
		//
		//System.out.print(x + "\n");
		//System.out.print(twice + "\n");
		//System.out.print(three);
		//
		//br.close();
		
		//2_4＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
//		BufferedReader br1 = new BufferedReader(new InputStreamReader(System.in));
//		int x = Integer.parseInt(br1.readLine()); //Xの標準入力
//		
//		BufferedReader br2 = new BufferedReader(new InputStreamReader(System.in));
//		int y = Integer.parseInt(br2.readLine()); //yの標準入力
//		
//		
//		int addAmount = x + y;
//		System.out.print("足し算の結果 : " 		 + addAmount +"\n");
//		
//		int subAmount = x - y;
//		System.out.print("引き算の結果 : " 		 + subAmount + "\n");
//		
//		int mulAmount = x * y;
//		System.out.print("掛け算の結果 : " 		 + mulAmount +"\n");
//		
//		int defAmount = x  / y;
//		System.out.print("割り算の商の結果 : "   + defAmount + "\n");
//		
//		int rimAmount = x % y;
//		System.out.print("割り算の余りの結果 : " + rimAmount);
//		
//		br1.close();
//		br2.close();
		
		
		//2_5＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
//		BufferedReader br1 = new BufferedReader(new InputStreamReader(System.in));
//     	int x = Integer.parseInt(br1.readLine()); //xの標準入力	
//        BufferedReader br2 = new BufferedReader(new InputStreamReader(System.in));
//   		int y = Integer.parseInt(br2.readLine()); //yの標準入力
//   		
//   		int avgAmount = (x + y) / 2;
//		System.out.print(avgAmount);
//		
//		br1.close();
//	    br2.close();
		
		//2_6＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		int age = Integer.parseInt(br.readLine());
		
		int totalDays = age * 365;
		System.out.print(totalDays);
		
		br.close();
		
	}
}